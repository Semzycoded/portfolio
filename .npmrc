# .npmrc - NPM configuration for Vercel deployment
# This setting allows npm to install packages even if they have peer dependency conflicts
# Required for compatibility between React 18, react-scripts, and other dependencies
legacy-peer-deps=true
